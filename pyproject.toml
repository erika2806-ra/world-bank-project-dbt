[project]
name = "world-bank-project-dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Erika Rasamoely", email = "rav_erika@yahoo.fr" }
]
requires-python = ">=3.12"
dependencies = [
    "db-dtypes>=1.7.1",
    "dbt-bigquery>=1.12.0",
    "joblib>=1.6.0",
    "pyarrow>=25.0.1",
    "python-dotenv>=1.2.3",
    "scikit-learn>=1.9.1",
]

[project.scripts]
world-bank-project-dbt = "world_bank_project_dbt:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
